import { cookies } from 'next/headers';
import BookingCard from '@/components/reserva/BookingCard';
import { CalendarDays, Filter } from 'lucide-react';
import Link from 'next/link';
import { getBookings } from '@/app/actions/bookings';


export default async function ReservasPage() {

         const response = await getBookings();
         const bookings = response.data;
        //   console.log("reservas :==> :  ", bookings)


  return (
    <div className="space-y-8">
      <header className="flex flex-col md:flex-row justify-between items-start md:items-center gap-4">
        <div>
          <div className="flex items-center gap-2 mb-1 mx-auto">
            <CalendarDays className="text-brand-gold" size={24} />
            <h1 className="text-2xl font-bold text-brand-black uppercase tracking-tight">
              Gestión de Reservas  
            </h1>
          </div>
          <p className="text-gray-500 text-sm">Mostrando {response.count} registros totales.</p>
        </div>

        <div className="flex gap-2 w-full md:w-auto">
          <button className="flex-1 md:flex-none flex items-center justify-center gap-2 border border-brand-gray bg-brand-white px-4 py-2 rounded-lg font-bold text-xs text-brand-black hover:bg-brand-gray/10">
            <Filter size={16} /> Filtrar
          </button>


        </div>
      </header> 

      {/* Grid de Reservas */}
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
        {bookings.length > 0 ? (
          bookings.map((booking: any) => (
            <BookingCard 
                        key={booking._id}
                        booking={booking}
                       />
          ))
        ) : (
          <div className="col-span-full py-20 text-center bg-brand-white rounded-xl border border-dashed border-brand-gray">
            <p className="text-gray-400 font-medium italic text-sm">No hay reservas registradas actualmente.</p>
          </div>
        )}
      </div>
    </div>
  );
}


